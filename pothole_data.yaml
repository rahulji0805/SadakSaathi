# Pothole detection dataset config
# Adjust path to your absolute dataset location

path: ./dataset       # root dir (contains images/ and labels/)
train: images/train
val: images/val

names:
  0: pothole
